# 主对照实验 · 预先登记

> **本文件在实验运行之前写定,写完即不再修改。** 之后跑出什么就报什么。
> 登记时间:2026-09-03
> 目的:防止"看到结果再决定怎么解释"这类事后合理化。

---

## 一、为什么要做实验C(承认前一个实验的设计错误)

实验A 用卷A 测出的结论是:**多角色配置与单 Agent 无显著差异**(B0 12% / B2 8% / B4 17%,标准差 ±7~12)。

复盘后我认为**这个测法本身对多 Agent 不公平,是我的出题错误**:

卷A 的 8 个雷**全部是"跨章节深度推理型"**(第 5.1 条与第 4.3 条矛盾、北极星指标被机制自身污染、样本量不足……)。而跨章节推理是**单个读者仔细读就能完成的任务** —— 多派三个人读同一份方案,不会让"发现两处矛盾"变容易。

**多 Agent 的设计目标从来不是深度,而是广度**:四个不同立场的评审,应当覆盖比单一视角更多的关注领域。

一个旁证:实验A 中 B1 单 Agent 只产出 5 条观点,B4 四角色产出 20 条 —— 四倍差距一直存在,但卷A 把所有雷都埋在"深度"一侧,广度优势完全没有被计分。

**结论:我用了一把量深度的尺子,去测一个为广度设计的系统。**

## 二、本实验要检验的假设

**H1(主假设)**:在一份缺陷横跨多个关注领域的方案上,多角色配置的**领域覆盖数**显著高于单 Agent。

**H2(次假设)**:给角色人设加上互相排他的禁令后,领域覆盖数进一步提高,且归因矩阵的对角线变亮。

H2 的依据:实验A 的归因矩阵显示四个角色关注面高度重叠(技术角色抓用户类雷比本职还多、商业类雷全组零命中)。既然角色不正交,本来就不该期待它们有差异。

## 三、被测对象:卷B(广度试卷)

**与卷A 的关键区别**:

| | 卷A | 卷B |
|---|---|---|
| 雷的类型 | 跨章节深度推理 | **单点可见** |
| 雷的分布 | 4 个类别 | **6 个互不重叠的关注领域** |
| 雷数 | 8 | **12(每领域 2 个)** |
| 考的能力 | 深度 | **广度** |

**刻意设计**:卷B 的每个雷**单独都很浅**(在方案的某一处单点阅读即可发现,不需要交叉比对),**但合起来极广**(横跨 6 个领域)。这就把"广度"从"深度"里隔离出来。

6 个领域:用户体验 / 技术实现 / 商业模型 / 风险合规 / 数据隐私 / 运营承接。

**为什么是 6 个而系统只有 4 个角色**:刻意让领域数多于角色数,避免"一个角色对一个领域"的过度贴合。四个角色的职责映射为:用户体验→用户画像;技术实现 + 数据隐私→技术可行性;商业模型 + 运营承接→商业化;风险合规→反方质疑者。

**防套话**:每个雷都配「命中要点」与「判为漏检的情形」,泛泛的"建议注意隐私""建议加强审核"一律判漏检,必须指出该领域内的具体问题。

## 四、对照组(输出模板全组统一,唯一变量是人设配置)

| 组 | 配置 | 调用数 |
|---|---|---|
| **C0** | 1 个通用 Agent + 跨章节模板 | 1 |
| **C1** | 同一个通用 Agent 跑 4 遍 + 主持人(算力对齐组) | 5 |
| **C2** | 4 角色(**旧人设**)+ 主持人 | 5 |
| **C3** | 4 角色(**新排他人设**)+ 主持人 | 5 |

**四组全部使用同一个跨章节输出模板** —— 这样"输出模板"这个变量被固定,组间差异只能来自人设配置。这是实验A 没做到的(那里 B2 用 full 模板、B5 用 cross 模板,存在混淆)。

对比关系:

| 对比 | 回答什么 |
|---|---|
| C0 → C1 | 把同一人设重复问 4 遍能带来多少提升(纯算力) |
| **C1 → C2** | **调用次数对齐下,角色化本身的净贡献** |
| **C2 → C3** | **人设排他性的贡献** |
| C0 → C3 | 完整产品 vs 直接问大模型 |

## 五、主指标

**领域覆盖数 = 6 个领域中,至少有 1 个雷被发现的领域数量。**

这才是多 Agent 应当被考核的东西。次指标仍报缺陷召回率、高危召回率、空泛观点率、引用有效率、观点数。

## 六、事前预测(关键:此处写死,不得事后修改)

| 组 | 预测领域覆盖数(满分 6) | 理由 |
|---|---|---|
| C0 单 Agent | **2~3** | 一次输出 4~8 条观点,注意力集中在最显眼的问题上,难以铺开 6 个领域 |
| C1 同人设×4 | **3~4** | 观点总数上去了,但四次都是同一视角,重复率高 |
| C2 4角色旧人设 | **4~5** | 视角分化带来覆盖提升,但人设重叠会造成浪费 |
| C3 4角色新人设 | **5~6** | 排他人设强制每个角色守住自己的领域 |

**同时预测归因矩阵**:C3 的对角线应明显亮于 C2。

## 七、判定规则(事前定死,防止事后挑口径)

1. **每组跑 3 次**,报均值 ± 标准差;领域覆盖数同时报"3 次并集"(累计能覆盖几个领域);
2. **H1 成立的判据**:C3 的领域覆盖数均值 ≥ C0 + 2 个领域,且两组标准差区间不重叠;
3. **H2 成立的判据**:C3 领域覆盖数 > C2,且 C3 归因矩阵对角线元素之和 > 非对角线之和;
4. **只跑一次实验,不重跑挑好看的那次**。若因限流等技术原因中断,重跑但必须报告中断原因;
5. **假设不成立时的处理**:如实报告 H1/H2 不成立,并在项目说明中把"多角色更有效"从卖点里删除,改为"多角色的作用是提升观点产出量而非覆盖广度"。**不修改雷清单、不修改判定标准、不挑选有利子集。**

## 八、已知局限(事前声明)

- 单份试卷、每组 3 次,样本量小;领域覆盖数的取值只有 0~6 共 7 档,分辨率有限;
- 卷B 由我一人出题,存在出题人偏差。但四组用的是同一套雷,偏差对各组一致,而全部结论都来自组间差值;
- 卷B 是为检验"广度"专门设计的,**结论不能外推为"多 Agent 在所有场景都更好"**,只能说"在缺陷横跨多领域的方案上"。

---

## 九、实验结果(留空,跑完后由脚本填写到 `实验记录_主对照.md`)

见 `实验记录_主对照.md`。本文件的预测部分不因结果而修改。
